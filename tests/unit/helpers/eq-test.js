import { eq } from '../../../helpers/eq';
import { module, test } from 'qunit';

module('Unit | Helper | eq');

// Replace this with your real tests.
test('it works', function(assert) {
  let param1 = 42;
  let param2 = 42;
  let result = eq([param1, param2]);
  assert.ok(result);
});
